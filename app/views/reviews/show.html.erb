<div style="text-align:center;">
<h1>Reviews Page</h1>
<p>Reviews for <%= @product.id %></p>

<% @product.reviews.each do |review| %>
	<p>
		<%= review.comment %>
		<br>
		<% if review.user != nil %>
			<em> by <%= review.user.email %></em>
			Added on: <%= review.created_at %>
		<% end %>
	</p>
<% end %>

<%= link_to 'Back', products_path %>
</div>